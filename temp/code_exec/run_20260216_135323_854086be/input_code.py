import subprocess, logging, sys, json; logging.basicConfig(level=logging.INFO, format='%(asctime)s - %(levelname)s - %(message)s'); cmd = ["python", "run_tests.py"]; proc = subprocess.run(cmd, capture_output=True, text=True); logging.info("Executed command: %s", " ".join(cmd)); logging.info("Return code: %s", proc.returncode); logging.info("Stdout:"); logging.info(proc.stdout); logging.info("Stderr:"); logging.info(proc.stderr); result = proc.stdout.count("PASS"); print(result)