import sympy as sp\n\nx = sp.symbols('x')\nf_sym = x**3 - x - 2\ndf_sym = sp.diff(f_sym, x)\n\ndef f(xv):\n    return sp.N(f_sym.subs(x, xv))\ndef df(xv):\n    return sp.N(df_sym.subs(x, xv))\n\ndef newton_raphson(x0, tol=1e-8, max_iter=100):\n    x = x0\n    for i in range(max_iter):\n        fx = f(x)\n        dfx = df(x)\n        if dfx == 0:\n            break\n        x_new = x - fx/dfx\n        if abs(x_new - x) < tol:\n            return i+1, x_new\n        x = x_new\n    return max_iter, x\n\ndef bisection(a, b, tol=1e-8, max_iter=100):\n    fa = f(a)\n    fb = f(b)\n    if fa*fb > 0:\n        raise ValueError('No sign change')\n    for i in range(max_iter):\n        c = (a+b)/2\n        fc = f(c)\n        if abs(b-a) < tol or abs(fc) < tol:\n            return i+1, c\n        if fa*fc < 0:\n            b, fb = c, fc\n        else:\n            a, fa = c, fa\n    return max_iter, (a+b)/2\n\nnewton_iter, newton_root = newton_raphson(1.0)\nbisection_iter, bisection_root = bisection(1.0, 2.0)\nresult = (newton_iter, bisection_iter)\nprint(result)