import pandas as pd\n\ndf = pd.read_csv('employment_data.csv')\n\n# Define treatment and post periods\ntreated = df[df['treated'] == 1]\ncontrol = df[df['treated'] == 0]\n\n# Compute employment change per state\ndf['emp_change'] = df.groupby('state')['employment'].diff()\n\n# Compute average change in post period\ntreated_post_change = treated[treated['post'] == 1]['emp_change'].mean()\ncontrol_post_change = control[control['post'] == 1]['emp_change'].mean()\ntreated_pre_change = treated[treated['post'] == 0]['emp_change'].mean()\ncontrol_pre_change = control[control['post'] == 0]['emp_change'].mean()\n\n# DiD estimator\ndid_effect = (treated_post_change - treated_pre_change) - (control_post_change - control_pre_change)\n\nresult = did_effect\nprint(result)\n